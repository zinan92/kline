# Required for A-share data
TUSHARE_TOKEN=your_tushare_pro_token

# Optional: database path (default: data/kline.db)
# KLINE_DB_PATH=data/kline.db

# Optional: server port (default: 8100)
# KLINE_PORT=8100
